AZURE_DEVOPS_ORG=your-org
AUTH_METHOD=pat
# base64 of "<email>:<pat>"
PERSONAL_ACCESS_TOKEN=

# host port the endpoint is published on
PORT=8080
# optional; if set, clients must send X-API-Key header with this value
MCP_PROXY_API_KEY=
